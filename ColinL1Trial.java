// CSUN FALL 22   // ColinL1Trial.java Trial Lab Exam-1 COMP 182 Oct. 19, 22// This program gets 4 integers n(<=30), m(>=5), p and x from// program argument, Inserts n random integers from p...p+m-1// in the arr[] and deletes duplicate of x// Prepared by: Gh. Dastghaiby Fard, import java.util.*;import java.io.*;  // class ColinL1Trialpublic class ColinL1Trial{ 	// class Variables	protected int count = 0, arr[]; 		 	// use prt for System.out to save typing	PrintStream prt = System.out;				  	// Insert x in after last element	public void insert(int x){		// local variables  		arr[count] = x;		count++;							} // end insert		  // if x is duplicate delete it and return no. of duplicatesprivate void deletealldups(int n){        // local variables        int p, q, x, y, k, cnt = 0;        p = 0;        while (p < count) {            x = arr[p]; //retrieve elemnt at p            q = p + 1;            while (q < count) {                y = arr[q]; //retrieve element at q                if (x == y){                    count--; //decrease # of elements in list                    //delete element at position q                    for (k = q; k < count; k++){                        arr[k] = arr[k+1];                    }                    cnt++; //increase number of duplicates                } else {                    q++;                }            }            p++;        }//end while        prt.print(arr[count]);        prt.printf("\n\t\t%d duplicates", cnt);    } // end deletedup    	// print arr[], formatted 10 elements per line	public void prtlst(){		// local variables		int i;		prt.printf("\n\tarr contents(%d):\n\t", count);		for (i = 0; i < count; i++){ 			prt.printf("%4d, ", arr[i]);			if((i+1) % 10 == 0) prt.printf("\n\t");		}// enf for			} // end prtlst	// insert n random elements in the list	private void process(int n, int m, int p, int x){		// local variables		int j, num; 		prt.printf("\tThis program gets 4 integers n(<=30), m(>=5), p and x from program argument"+		"\n\tInserts %d random integers from %d...%d, in the arr[] and deletes duplicate of %d:", n, p, m+p-1, x);		prt.printf("\n\t\tTo compile: javac ColinL1Trial.java" +		"\n\t\tTo execute: java  ColinL1Trial n  m  p  x"+		"\n\t\tExample:    java  ColinL1Trial 20 8 50 52"); 	  				if ( n > 30 || m < 5){			prt.printf("\n\tInvalid values for n or m");			return;					}// end if				// Allocate Space for array		 	 		arr = new int[30];				// create an instance of class Random		Random rnd = new Random();				// Generate n random integers from p...p+m		// and insert in arr[]		for(j = 1; j <= n; j++){			// Generate a random integers from p...p+m			num = rnd.nextInt(m) + p;   			insert(num); //insert x in arr[]		} // end				// print arr[], formatted 10 elements per line		prtlst();				// if x is duplicate in arr[], delete it and print no. of duplicates      deletealldups();				// print arr[], formatted 10 elements per line		prtlst();//print list elements	} // end process		  	// main method	public static void main(String args[]) throws Exception{		// declare variables		int n, m, p, x, cnt;		// get no. of program arguments		cnt = args.length; 				if (cnt < 3){			System.out.printf("\n\n\tOOPS Invalid No. of aguments! EXIT.\n");			System.out.printf("\tThis program gets 4 integers n(<=30), m(>=5), p and x from program argument"+			"\n\tExample: java ColinL1Trial 20 8 50 52"); 	  			return;		} // end if								// get value of arguments		n = Integer.parseInt(args[0]);		m = Integer.parseInt(args[1]);		p = Integer.parseInt(args[2]);		x = Integer.parseInt(args[3]);		//create an instance of a class		ColinL1Trial lst = new ColinL1Trial();				// Call process method		lst.process(n, m, p, x);							//Pleas replace G. Dastghaibyfard with your name in next line				System.out.printf("\n\n\tAuthor: A. Colin Date: %s\n",		java.time.LocalDate.now()); 	} // end main} // end class ColinL1Trial